import React from "react";
import { Card, CardContent } from "@material-ui/core";

const PropertyContactForm = () => {
  return (
    <>
      <Card elevation={0}>
        <CardContent>VHV</CardContent>
      </Card>
    </>
  );
};
export default React.memo(PropertyContactForm);
